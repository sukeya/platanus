# Benchmark Visualization

This page links to the interactive benchmark chart generated from `bench_result_with_absl.json`.

Open the standalone HTML file here:

[View benchmark chart](benchmark-assets/bench_result_with_absl.html)

To regenerate the embedded HTML:

```bash
python3 benchmark/compare_benchmarks.py bench_result_with_absl.json -o docs/benchmark-assets/bench_result_with_absl.html
```
